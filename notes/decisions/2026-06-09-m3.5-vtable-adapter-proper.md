# M3.5 — full vtable adapter (cl_refvulkan.c)

**Date:** 2026-06-09
**Phase:** Vulkan Phase 2, Milestone 3.5 (ABI carry-over from M3)
**Supersedes:** `notes/decisions/2026-06-08-m2.4-vtable-adapter-shortcut.md`
**Commits:** `a3b9aef..34361cc` (8 commits, all on `macos-arm64-vulkan`)

## Context

M3 closed with both renderer DLLs link-clean. First runtime test of the
Vulkan DLL revealed R_Init segfaults before vkCreateInstance — the
renderer's `ri.Cmd_AddCommand` arrived at the engine with an empty
string, repeating 8 times before crashing on the 9th.

Root cause: refImport_t vocabulary mismatch. M2.4 chose a 12-line
conditional include hoping the two structs were nested supersets. They
turn out to be **disjoint vocabularies** (~13 fields in small only, ~25
in big only, on top of the shared base). The conditional was also a
no-op because `-DBUILD_RENDERER_VULKAN` never reached engine TUs anyway
— but even if it had, the engine code populating `ri.Z_Malloc`,
`ri.IN_Init`, `ri.Sys_GLimpInit` etc. would have failed to compile
against the big struct that doesn't have those names.

## Decision

Build a proper RealRTCW-authored translator at `code/client/cl_refvulkan.c`:

1. Engine code throughout sees the small RTCW refImport_t consistently
   (revert M2.4's conditional include to always pull the small one).
2. `cl_refvulkan.c` includes only the big Q3e refImport_t in isolation
   from `client.h`, building it from scratch by assigning engine
   globals directly to big-struct slots (NOT by copying from engine's
   small `ri`).
3. `CL_InitRef` under `BUILD_RENDERER_VULKAN` calls
   `CL_BuildVulkanRefImport()` and hands the result to `GetRefAPI`.

Slot categories in the translator:
- **Direct aliases** (~25 slots): Cmd_*, FS_*, Cvar_* (Q3e and RTCW
  share these names + signatures, possibly with explicit narrow casts).
- **Wrappers** (~7 slots): Microseconds (×1000 from Sys_Milliseconds),
  Malloc + FreeAll (local pointer tracker since Z_TagMalloc / Z_FreeTags
  in common.c are `#if 0`'d dead code), Hunk_Alloc / Hunk_AllocateTempMemory
  / Hunk_AllocDebug (size_t → int narrowing wrappers; arm64 AAPCS makes
  the fn-ptr cast usually work but it's silent UB), FS_ReadFile
  (long → int return narrowing).
- **Vulkan window glue** (4 slots): VK_CreateSurface (SDL3 via engine's
  global SDL_window), VK_GetInstanceProcAddr (PFN cached + passthrough),
  VKimp_Init (verify SDL_window exists; fatal if not), VKimp_Shutdown
  (no-op).
- **Intentionally NULL** (~23 slots): Q3e-only slots the renderer is
  unlikely to call from RTCW gameplay (Add*Light, FinishBloom, GLimp_*
  family, etc.). Listed in a comment block for grep-discoverability.
  If M4 triage shows a NULL deref on one of these, add a stub then —
  not pre-stub for hypothetical callers.

## Alternatives considered

- **A' (translator on renderer side)**: edit `code/renderervk/tr_init.c`
  to call engine names directly. Rejected — breaks vendor-prefix
  convention, makes future Q3e re-vendor painful.
- **B (refactor engine to Q3e vocabulary)**: rewrite engine code from
  `ri.AddCoronaToScene` to Q3e equivalents. Rejected — extensive
  engine code churn, risk of dropping RTCW-specific features.
- **C (drop OpenGL renderer)**: engine sees big struct only. Rejected
  — sacrifices OpenGL fallback that ragnar relies on while Vulkan
  matures.
- **D (extend engine refImport_t to superset)**: extend small struct
  with Q3e-only fields. Rejected after deeper review — required
  editing vendored `code/renderercommon/tr_public.h`, which would
  break vendor-prefix convention same as A'. Also raised
  feature-parity questions because the disjoint vocabularies imply
  feature-set, not just naming, divergence.

## Why position A (translator) over D (superset)

- **Vendored code untouched** — preserves vendor convention, makes
  future Q3e re-vendor a clean operation.
- **Upstream PR cleanness** — the diff for wolfetplayer/RealRTCW is
  one new file + ~10 lines in cl_main.c. Reviewable as «do you want a
  Vulkan adapter?» rather than structural surgery.
- **Single audit surface** — when M5+ effects don't render, the
  translation logic is in one file.
- **OpenGL fallback intact** — engine code path identical to before
  for OpenGL DLL load.

## Trade-off

- Adapter is one more file to maintain. ~400 lines of indirection.
- Indirection adds one extra function call to every renderer→engine
  callback (negligible perf, but real).
- Stubs for NULL slots will accumulate as M4-M5 triage discovers them.
- The build-system trick (`override CFLAGS +=`) is non-obvious and
  must be documented — without it, the per-target define silently
  no-ops (which masked an M3 latent bug for a session).

## Revisit if

- A future re-vendor of Q3e changes the big struct shape significantly
  — update the adapter, not the engine.
- Future renderer (e.g. Metal native) needs another translator — copy
  this file as a template.
- Engine moves to Q3e vocabulary natively (Phase 4 hypothesis) — drop
  the adapter, but that's an explicit re-architecture decision.

## Runtime verification

Captured at `docs/vulkan-phase2/m3.5-first-clean-init.log` (commit
`34361cc`):

Before M3.5:
- `Trying to load "renderer_sp_vulkan_arm64.dylib"...`
- `---- Renderer Initialization Complete ----`
- `----- R_Init -----`
- `Cmd_AddCommand:  already defined` × 8 (empty names)
- segfault

After M3.5:
- `Trying to load "renderer_sp_vulkan_arm64.dylib"...`
- `---- Renderer Initialization Complete ----`
- `----- Client Initialization Complete -----`
- segfault later in startup (M4 work)

The disappearance of the empty-name `Cmd_AddCommand` spam proves the
vtable now aligns — renderer calls the engine function it intended
to call, with real string arguments.

## Companion finding: M3 latent build bug (fixed retroactively here)

`Makefile:3082-3083` carried `$(B)/rendv/sdl_glimp.o: CFLAGS +=
-DBUILD_RENDERER_VULKAN` since M3 closure. But the `release:` /
`debug:` targets re-invoke `$(MAKE)` with `CFLAGS="..."` on the
command line, which silently voids subsequent target-specific
`CFLAGS +=` Makefile assignments per GNU Make precedence rules. So
`-DBUILD_RENDERER_VULKAN` never actually reached these recipes; the
`#ifdef BUILD_RENDERER_VULKAN`-guarded `RealRTCW_VkBridgeInit()` call
in sdl_glimp.c was dead code all along. M3 «closed» on link-clean
without runtime verification, so this hid until M3.5.

Fix: `override CFLAGS += -DBUILD_RENDERER_VULKAN` on all three
target-specific assignments. `override` reasserts the Makefile's
intent against the command-line CFLAGS the submake was launched with.

Pattern recorded in `notes/reference/engine-map.md` so future
per-target CFLAGS additions don't fall into the same trap.

## See also

- `code/client/cl_refvulkan.h`, `code/client/cl_refvulkan.c`
- `code/client/cl_main.c:3382` (CL_InitRef hook)
- `notes/decisions/2026-06-08-m2.4-vtable-adapter-shortcut.md` (superseded)
- `notes/reference/engine-map.md` — Renderer-ABI shim layer section
- `docs/vulkan-phase2/m3.5-first-clean-init.log` — runtime verification
